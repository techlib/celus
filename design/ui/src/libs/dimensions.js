const explicitDimensionCount = 7;
const explicitDimensions = [];

for (let i = 0; i < explicitDimensionCount; i++) {
  explicitDimensions.push(`dim${i}`);
}

export { explicitDimensions, explicitDimensionCount };
